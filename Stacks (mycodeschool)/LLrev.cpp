#include<iostream>
#include<string>
using namespace std;

struct Node{
    int data;
    Node* next;
};
Node* head;
void Reverse()
{
    if(head==NULL)return;
    stack <Node* int> s;
    Node* temp=head;
    while(temp!=NULL){
        s.push
    }
}
void insert(int x)
{
    Node* temp= new Node;
    temp->data=x;
    temp->next=NULL;
    if(head==NULL){
        head=temp;
        return;
    }
    Node* temp1=head;
    while(temp1->next!=NULL){
        temp1=temp1->next;
    }
}
int main()
{
    Node=NULL;
}